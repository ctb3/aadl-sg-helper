node_modules/
.env

# Deploy-time generated payloads (contain the PIN + GCP key)
infra/.lambda-env.json
infra/.role-policy.json
infra/.ecr-repo-policy.json

# Terraform: state lives in S3 (bootstrap's first apply is local + migrated away)
**/.terraform/
*.tfstate
*.tfstate.*

# One-time old-account session download staging (infra/README.md cutover step)
sessions-archive/

# Generated / cached artifacts
data/prepped/
data/crops/
data/cache/
out/

# Test data (photos + hand labels are local, potentially private)
data/images/
data/labels.csv
data/manual cropped/
